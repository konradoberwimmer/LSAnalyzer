<UserControl x:Class="LSAnalyzer.Views.CustomControls.Percentiles"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:viewModels="clr-namespace:LSAnalyzer.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:RequestAnalysis, IsDesignTimeCreatable=True}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.Resources>
            <Style x:Key="InvisibleButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Grid Background="{TemplateBinding Background}">
                                <ContentPresenter Content="{TemplateBinding Content}"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ItemsControl Grid.Row="1" Margin="0,5,0,0" ItemsSource="{Binding Percentiles}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Width="300" HorizontalAlignment="Left"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0,0,15,0">
                        <TextBlock Text="{Binding Value, StringFormat={}{0:0.00#}}" Margin="0,0,0,0"/>
                        <Button Background="Transparent" BorderBrush="Transparent" Margin="3,0,0,0" Padding="0" Command="{Binding RelativeSource={RelativeSource AncestorType=ItemsControl}, Path=DataContext.RemovePercentileCommand}" CommandParameter="{Binding}">
                            <Button.Content>
                                <fa:IconImage Icon="CircleXmark" Height="15" Foreground="Red"/>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBox Width="50" Text="{Binding NewPercentile, UpdateSourceTrigger=PropertyChanged}" KeyUp="TextBoxNewPercentile_OnKeyUp"/>
            <Button Style="{StaticResource InvisibleButtonStyle}" Margin="3,0,0,0" Padding="0" IsEnabled="{Binding NewPercentile.Length}" Command="{Binding AddPercentileCommand}">
                <Button.Content>
                    <fa:IconImage Icon="CirclePlus" Height="15" Foreground="Green" />
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
