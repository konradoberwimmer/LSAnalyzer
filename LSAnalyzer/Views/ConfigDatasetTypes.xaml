<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converter="clr-namespace:LSAnalyzer.ViewModels.ValueConverter"
        xmlns:local="clr-namespace:LSAnalyzer.Views"
        xmlns:ViewModels="clr-namespace:LSAnalyzer.ViewModels" x:Class="LSAnalyzer.Views.ConfigDatasetTypes"
        mc:Ignorable="d"
        Title="Dataset Types" WindowStartupLocation="CenterScreen" Closing="WindowClosing" MinWidth="900" MinHeight="550">
    <Window.DataContext>
        <ViewModels:ConfigDatasetTypes/>
    </Window.DataContext>
    <Window.Resources>
        <converter:BooleanAnd x:Key="BooleanAndConverter"/>
        <converter:DictionaryHasElementToBool x:Key="DictionaryHasElementToBoolConverter"/>
        <converter:IndexToBool x:Key="IndexToBoolConverter"/>
        <local:WindowSettings x:Key="WindowSettings"/>
    </Window.Resources>
    <Window.Width>
        <Binding Source="{StaticResource WindowSettings}" Path="WidthConfigDatasetTypes" Mode="TwoWay"/>
    </Window.Width>
    <Window.Height>
        <Binding Source="{StaticResource WindowSettings}" Path="HeightConfigDatasetTypes" Mode="TwoWay"/>
    </Window.Height>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="comboBoxSelectDatasetType" Grid.Column="0" VerticalAlignment="Top" Width="312" ItemsSource="{Binding DatasetTypes}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedDatasetType, Mode=TwoWay}"/>
            <Button Grid.Column="1" Content="New" HorizontalAlignment="Left" Margin="5,0,0,0" VerticalAlignment="Top" Height="22" Width="78" Command="{Binding NewDatasetTypeCommand, Mode=OneWay}"/>
            <Button Grid.Column="2" Content="Import ..." HorizontalAlignment="Left" Margin="5,0,0,0" VerticalAlignment="Top" Height="22" Width="78" Click="ButtonImportDatasetType_Click"></Button>
            <Button Grid.Column="3" Content="Remove" HorizontalAlignment="Right" Margin="30,0,0,0" VerticalAlignment="Top" Height="22" Width="78" IsEnabled="{Binding SelectedIndex, ElementName=comboBoxSelectDatasetType, Converter={StaticResource IndexToBoolConverter}}" Click="ButtonRemoveDatasetTypeClick"/>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" HorizontalAlignment="Left"/>
            <TextBox Grid.Row="0" Grid.Column="1" Margin="5,0,0,0" TextWrapping="Wrap" Text="{Binding SelectedDatasetType.Name, UpdateSourceTrigger=PropertyChanged}">
                <ToolTipService.ToolTip>
                    <TextBlock Text="{Binding SelectedDatasetType.Errors[Name]}" Foreground="Red"/>
                </ToolTipService.ToolTip>
                <ToolTipService.IsEnabled>
                    <Binding Path="SelectedDatasetType.Errors" Converter="{StaticResource ResourceKey=DictionaryHasElementToBoolConverter}" ConverterParameter="Name"/>
                </ToolTipService.IsEnabled>
            </TextBox>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Group:" HorizontalAlignment="Left" Margin="0,5,0,0"/>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="5,5,0,0" TextWrapping="Wrap" Text="{Binding SelectedDatasetType.Group, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Description:" Grid.Row="2" Grid.Column="0" HorizontalAlignment="Left" Margin="0,5,0,0"/>
            <TextBox Grid.Row="2" Grid.Column="1" Margin="5,5,0,0" TextWrapping="Wrap" Text="{Binding SelectedDatasetType.Description, UpdateSourceTrigger=PropertyChanged}"/>
            <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,5,0,0" Orientation="Horizontal">
                <CheckBox Content="Auto-encapsulate regular expressions" IsChecked="{Binding SelectedDatasetType.AutoEncapsulateRegex}"/>
                <ContentControl ContentTemplate="{StaticResource InfoTipInline}" ToolTip="If checked, any regular expression XY will be treated as ^XY$ when used."/>
            </StackPanel>
            <TextBlock Text="Weight variable:" Grid.Row="4" Grid.Column="0" HorizontalAlignment="Left" Margin="0,5,0,0"/>
            <TextBox Grid.Row="4" Grid.Column="1" Margin="5,5,0,0" TextWrapping="Wrap" Text="{Binding SelectedDatasetType.Weight, UpdateSourceTrigger=PropertyChanged}" Width="200" HorizontalAlignment="Left">
                <ToolTipService.ToolTip>
                    <TextBlock Text="{Binding SelectedDatasetType.Errors[Weight]}" Foreground="Red"/>
                </ToolTipService.ToolTip>
                <ToolTipService.IsEnabled>
                    <Binding Path="SelectedDatasetType.Errors" Converter="{StaticResource ResourceKey=DictionaryHasElementToBoolConverter}" ConverterParameter="Weight"/>
                </ToolTipService.IsEnabled>
            </TextBox>
            <StackPanel Grid.Row="5" Grid.Column="0" Margin="0,5,0,0" Orientation="Horizontal">
                <TextBlock Text="ID variable:"/>
                <ContentControl ContentTemplate="{StaticResource InfoTipInline}" ToolTip="Providing a unique ID variable is not necessary but will guarantee sorting and correct associations between cases when using MI indicator variable."/>
            </StackPanel>
            <TextBox Grid.Row="5" Grid.Column="1" Margin="5,5,0,0" TextWrapping="Wrap" Text="{Binding SelectedDatasetType.IDvar, UpdateSourceTrigger=PropertyChanged}" Width="200" HorizontalAlignment="Left"/>
        </Grid>
        <GroupBox x:Name="GroupBoxMI" Grid.Row="2" Grid.Column="0" Margin="0,10,0,0" VerticalAlignment="Stretch">
            <GroupBox.Header>
                <TextBlock FontWeight="DemiBold"><Run Text="Multiple Imputation / Plausible Values"/></TextBlock>
            </GroupBox.Header>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0">
                    <TextBlock TextWrapping="Wrap" Text="Number of multiple imputations or plausible values:" Margin="0,5,0,0"/>
                    <TextBox Width="50" Text="{Binding SelectedDatasetType.NMI, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left">
                        <ToolTipService.ToolTip>
                            <TextBlock Text="{Binding SelectedDatasetType.Errors[NMI]}" Foreground="Red"/>
                        </ToolTipService.ToolTip>
                        <ToolTipService.IsEnabled>
                            <Binding Path="SelectedDatasetType.Errors" Converter="{StaticResource ResourceKey=DictionaryHasElementToBoolConverter}" ConverterParameter="NMI"/>
                        </ToolTipService.IsEnabled>
                    </TextBox>
                    <Rectangle Height="1" Stroke="Gray" Margin="0,10,0,10"/>
                    <TextBlock TextWrapping="Wrap" Text="Variable indicating imputation count:"/>
                    <TextBox TextWrapping="Wrap" Text="{Binding SelectedDatasetType.MIvar, UpdateSourceTrigger=PropertyChanged}" Width="200" HorizontalAlignment="Left">
                        <ToolTipService.ToolTip>
                            <TextBlock Text="{Binding SelectedDatasetType.Errors[MIvar]}" Foreground="Red"/>
                        </ToolTipService.ToolTip>
                        <ToolTipService.IsEnabled>
                            <Binding Path="SelectedDatasetType.Errors" Converter="{StaticResource ResourceKey=DictionaryHasElementToBoolConverter}" ConverterParameter="MIvar"/>
                        </ToolTipService.IsEnabled>
                    </TextBox>
                    <TextBlock TextWrapping="Wrap" Text="OR"/>
                    <TextBlock TextWrapping="Wrap" Text="Variables with plausible values:"/>
                </StackPanel>
                <Border Grid.Row="1" x:Name="BorderPVvars" VerticalAlignment="Stretch">
                    <DataGrid ItemsSource="{Binding SelectedDatasetType.PVvarsList}" AutoGenerateColumns="False" Height="{Binding ElementName=BorderPVvars, Path=ActualHeight}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Regex" Binding="{Binding Regex}" Width="150"/>
                            <DataGridTextColumn Header="Display name" Binding="{Binding DisplayName}" Width="150"/>
                            <DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="150"/>
                            <DataGridCheckBoxColumn Header="Mandatory" Binding="{Binding Mandatory}" Width="75"/>
                        </DataGrid.Columns>
                        <ToolTipService.ToolTip>
                            <TextBlock Text="{Binding SelectedDatasetType.Errors[PVvarsList]}" Foreground="Red"/>
                        </ToolTipService.ToolTip>
                        <ToolTipService.IsEnabled>
                            <Binding Path="SelectedDatasetType.Errors" Converter="{StaticResource ResourceKey=DictionaryHasElementToBoolConverter}" ConverterParameter="PVvarsList"/>
                        </ToolTipService.IsEnabled>
                    </DataGrid>
                </Border>
            </Grid>
        </GroupBox>
        <GroupBox Grid.Row="2" Grid.Column="1"  Margin="10,10,0,0" VerticalAlignment="Top" Height="{Binding ElementName=GroupBoxMI, Path=ActualHeight}">
            <GroupBox.Header>
                <TextBlock FontWeight="DemiBold"><Run Text="Replicate Weights"/></TextBlock>
            </GroupBox.Header>
            <StackPanel Margin="0,5,0,0">
                <TextBlock TextWrapping="Wrap" Text="Replication variance adjustment factor (argument fayfac in BIFIEsurvey):" Width="225" HorizontalAlignment="Left"/>
                <TextBox TextWrapping="Wrap" Width="50" Text="{Binding SelectedDatasetType.FayFac}" HorizontalAlignment="Left">
                    <ToolTipService.ToolTip>
                        <TextBlock Text="{Binding SelectedDatasetType.Errors[FayFac]}" Foreground="Red"/>
                    </ToolTipService.ToolTip>
                    <ToolTipService.IsEnabled>
                        <Binding Path="SelectedDatasetType.Errors" Converter="{StaticResource ResourceKey=DictionaryHasElementToBoolConverter}" ConverterParameter="FayFac"/>
                    </ToolTipService.IsEnabled>
                </TextBox>
                <Rectangle Height="1" Stroke="Gray" Margin="0,10,0,10"/>
                <TextBlock TextWrapping="Wrap" Text="Variables with replicate weights (regex):"/>
                <TextBox TextWrapping="Wrap" Text="{Binding SelectedDatasetType.RepWgts, UpdateSourceTrigger=PropertyChanged}" Width="200" HorizontalAlignment="Left">
                    <ToolTipService.ToolTip>
                        <TextBlock Text="{Binding SelectedDatasetType.Errors[RepWgts]}" Foreground="Red"/>
                    </ToolTipService.ToolTip>
                    <ToolTipService.IsEnabled>
                        <Binding Path="SelectedDatasetType.Errors" Converter="{StaticResource ResourceKey=DictionaryHasElementToBoolConverter}" ConverterParameter="RepWgts"/>
                    </ToolTipService.IsEnabled>
                </TextBox>
                <TextBlock TextWrapping="Wrap" Text="OR"/>
                <TextBlock TextWrapping="Wrap" Text="Variable indicating jackknife zones:"/>
                <TextBox TextWrapping="Wrap" Text="{Binding SelectedDatasetType.JKzone, UpdateSourceTrigger=PropertyChanged}" Width="200" HorizontalAlignment="Left">
                    <ToolTipService.ToolTip>
                        <TextBlock Text="{Binding SelectedDatasetType.Errors[JKzone]}" Foreground="Red"/>
                    </ToolTipService.ToolTip>
                    <ToolTipService.IsEnabled>
                        <Binding Path="SelectedDatasetType.Errors" Converter="{StaticResource ResourceKey=DictionaryHasElementToBoolConverter}" ConverterParameter="JKzone"/>
                    </ToolTipService.IsEnabled>
                </TextBox>
                <TextBlock TextWrapping="Wrap" Text="Variable indicating inclusion/exclusion within jackknife zone:" Width="225" HorizontalAlignment="Left"/>
                <TextBox TextWrapping="Wrap" Text="{Binding SelectedDatasetType.JKrep, UpdateSourceTrigger=PropertyChanged}" Width="200" HorizontalAlignment="Left">
                    <ToolTipService.ToolTip>
                        <TextBlock Text="{Binding SelectedDatasetType.Errors[JKrep]}" Foreground="Red"/>
                    </ToolTipService.ToolTip>
                    <ToolTipService.IsEnabled>
                        <Binding Path="SelectedDatasetType.Errors" Converter="{StaticResource ResourceKey=DictionaryHasElementToBoolConverter}" ConverterParameter="JKrep"/>
                    </ToolTipService.IsEnabled>
                </TextBox>
                <CheckBox Content="Use reverse inclusion/exclusion?" Margin="0,5,0,5" IsChecked="{Binding SelectedDatasetType.JKreverse}"/>
            </StackPanel>
        </GroupBox>
        <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Export ..." IsEnabled="{Binding SelectedIndex, ElementName=comboBoxSelectDatasetType, Converter={StaticResource IndexToBoolConverter}}" HorizontalAlignment="Left" Height="22" Width="78" Click="ButtonExportDatasetType_Click"/>
            <Button Grid.Column="1" Content="Save" HorizontalAlignment="Right" Height="22" Width="78" Command="{Binding SaveSelectedDatasetTypeCommand, Mode=OneWay}">
                <Button.IsEnabled>
                    <MultiBinding Converter="{StaticResource BooleanAndConverter}">
                        <Binding Path="SelectedIndex" ElementName="comboBoxSelectDatasetType" Converter="{StaticResource IndexToBoolConverter}"/>
                        <Binding Path="SelectedDatasetType.IsChanged" FallbackValue="false"/>
                    </MultiBinding>
                </Button.IsEnabled>
            </Button>
        </Grid>
    </Grid>
</Window>
