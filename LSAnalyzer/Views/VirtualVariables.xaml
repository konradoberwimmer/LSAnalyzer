<Window x:Class="LSAnalyzer.Views.VirtualVariables"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:LSAnalyzer.Views"
        xmlns:viewModels="clr-namespace:LSAnalyzer.ViewModels"
        xmlns:converter="clr-namespace:LSAnalyzer.ViewModels.ValueConverter"
        xmlns:models="clr-namespace:LSAnalyzer.Models"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:VirtualVariables, IsDesignTimeCreatable=True}"
        Title="VirtualVariables" Height="600" Width="1000" MinHeight="600" MinWidth="800" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <converter:NullToBool x:Key="NullToBoolConverter"/>
        <converter:VirtualVariableTypeToName x:Key="VirtualVariableTypeToNameConverter"/>
        <Style x:Key="ComboBoxSelectOverlay" TargetType="TextBlock">
            <Setter Property="Grid.ZIndex" Value="10"/>
            <Setter Property="Foreground" Value="{x:Static SystemColors.GrayTextBrush}"/>
            <Setter Property="Margin" Value="6,4,10,0"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="Visibility" Value="Hidden"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <TextBlock Text="{Binding CurrentFileName, StringFormat={}Virtual variables for file {0}}"/>
                <TextBlock Text="{Binding CurrentDatasetTypeName, StringFormat={} ({0})}"/>
            </StackPanel>
            <Border Grid.Row="1" x:Name="BorderDataGridVirtualVariables" Margin="0,5,0,0">
                <DataGrid ItemsSource="{Binding CurrentVirtualVariables}" SelectedItem="{Binding SelectedVirtualVariable}" SelectionMode="Single" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Name}" Header="Type"/>
                        <DataGridTextColumn Binding="{Binding Name}" Header="Variable name(s)"/>
                        <DataGridTextColumn Binding="{Binding ForFileName}" Header="For file name"/>
                        <DataGridCheckBoxColumn Binding="{Binding ForDatasetTypeId, Converter={StaticResource NullToBoolConverter}}" Header="For dataset type"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
            <Grid Grid.Row="2" Margin="0,5,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ComboBox x:Name="ComboBoxSelectedType" Grid.Column="0" ItemsSource="{Binding VirtualVariableTypes}" Width="120" HorizontalAlignment="Left">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource VirtualVariableTypeToNameConverter}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBlock Grid.Column="0" DataContext="{Binding ElementName=ComboBoxSelectedType, Path=SelectedItem}" Text="New ..." Style="{StaticResource ComboBoxSelectOverlay}"/>
                <Button Grid.Column="1" Content="Remove" Width="80" Height="22" HorizontalAlignment="Right" IsEnabled="{Binding SelectedVirtualVariable, Converter={StaticResource NullToBoolConverter}}"/>
            </Grid>
            <ContentControl Grid.Row="3" Content="{Binding SelectedVirtualVariable}" HorizontalAlignment="Stretch" Margin="0,5,0,0" BorderThickness="0,1,0,0">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type models:VirtualVariableCombine}">
                        <TextBlock>HI!</TextBlock>
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
        </Grid>
        <Grid Grid.Column="1" Margin="5,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Preview:"/>
                <Button Grid.Column="1" Margin="5,0,0,0" Width="16" Height="16" HorizontalAlignment="Right" IsEnabled="{Binding SelectedVirtualVariable, Converter={StaticResource NullToBoolConverter}}">
                    <Button.Content>
                        <fa:IconBlock Icon="Redo"/>
                    </Button.Content>
                </Button>
            </Grid>
            <DataGrid Grid.Row="1" Margin="0,5,0,0" MinWidth="200" ItemsSource="{Binding Preview}" AutoGenerateColumns="True" IsReadOnly="True"/>
        </Grid>
    </Grid>
</Window>
